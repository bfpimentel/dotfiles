[user]
  name = Bruno Pimentel
  email = hello@bruno.so
  username = bfpimentel

[core]
  editor = nvim

[init]
  defaultBranch = main

[merge]
  tool = vimdiff

[mergetool "vimdiff"]
  path = nvim
  cmd = nvim -d $LOCAL $REMOTE $MERGED -c '$wincmd w' -c '$wincmd J'
# vim: ft=dosini :
